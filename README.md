# my shit bot

immune to drek

to run:

`npm install` probably forgot to save some packages

create a config.json file with this stuff:

```
{
  prefix:'!',
  token:'ya bot token'
}
```

then

`node run dev`

enjoy
